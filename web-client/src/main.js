import { createApp } from 'vue';
import App from './App.vue';
import './style.css';

// Import Bootstrap JS for dynamic UI elements (dropdowns, modals, etc.)
import 'bootstrap';

createApp(App).mount('#app');
